package sql.demo;

public class SQLInstructions {

    /*   CREATE SCHEMA `ip_addreses` ;
CREATE TABLE `ip_addreses`.`ip_add` (
  `id_of_IP` INT NOT NULL AUTO_INCREMENT,
  `IP addresses` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`id_of_IP`),
  UNIQUE INDEX `IP addresses_UNIQUE` (`IP addresses` ASC) VISIBLE);
*/
}
